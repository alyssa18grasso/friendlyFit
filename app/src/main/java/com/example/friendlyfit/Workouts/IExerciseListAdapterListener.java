package com.example.friendlyfit.Workouts;

public interface IExerciseListAdapterListener {
    // position is the position of the item that got clicked in the recycler view list
    void exerciseListDeletePressed(int position);
}
